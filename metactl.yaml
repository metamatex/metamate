v0:
  gen:
    sdks:
      - name: go_httpjson_service
        args:
          name: mastodon
          package: github.com/metamatex/metamate
        endpoints: [GetPostFeeds, GetSocialAccounts, GetPosts]
      - name: go_httpjson_service
        args:
          name: kubernetes
          package: github.com/metamatex/metamate
        endpoints: [GetServices]
      - name: go_httpjson_client
        args:
          package: github.com/metamatex/metamate
        endpoints:
          - GetDummies
          - GetBlueDummies
          - GetServices
          - GetSocialAccounts
          - LookupService
          - GetPostFeeds
          - PipeDummies
          - GetPosts
