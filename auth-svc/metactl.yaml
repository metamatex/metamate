v0:
  gen:
    sdks:
      - names: [go_httpjson_server]
        data:
          package: github.com/metamatex/auth-svc
        endpoints:
          names:
            or: [VerifyToken, AuthenticateClientAccount, PipeClientAccounts]
      - names: [go_httpjson_client]
        data:
          package: github.com/metamatex/auth-svc
        endpoints:
          names:
            or: [GetClientAccounts]